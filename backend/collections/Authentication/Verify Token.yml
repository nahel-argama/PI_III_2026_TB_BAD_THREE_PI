info:
  name: Verify Token
  type: http
  seq: 3

http:
  method: POST
  url: "{{host}}/api/auth/verify/"
  body:
    type: json
    data: |
      {
        "token": "{{access}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
