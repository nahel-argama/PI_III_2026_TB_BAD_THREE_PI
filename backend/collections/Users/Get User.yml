info:
  name: Get User
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/api/users/:user_id/"
  params:
    - name: user_id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
