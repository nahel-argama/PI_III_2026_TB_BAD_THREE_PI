info:
  name: Partial Update User
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{host}}/api/users/:user_id/"
  params:
    - name: user_id
      value: "1"
      type: path
  body:
    type: json
    data: |
      {
        "name": "Jane"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
