info:
  name: Generate image
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/api/default-product-images/:external-id/generate-image"
  params:
    - name: external-id
      value: "75"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
