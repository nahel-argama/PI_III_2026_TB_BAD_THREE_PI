info:
  name: Get Order Item
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/api/order-items/:order_item_id/"
  params:
    - name: order_item_id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
