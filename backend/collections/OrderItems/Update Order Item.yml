info:
  name: Update Order Item
  type: http
  seq: 4

http:
  method: PUT
  url: "{{host}}/api/orders/:order_id/items/:order_item_id/"
  params:
    - name: order_id
      value: "1"
      type: path
    - name: order_item_id
      value: "1"
      type: path
  auth: inherit
  body:
    type: json
    data: |
      {
        "product": 1,
        "quantity": 15
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
