info:
  name: Update Order Item
  type: http
  seq: 4

http:
  method: PUT
  url: "{{host}}/api/order-items/:order_item_id/"
  params:
    - name: order_item_id
      value: "1"
      type: path
  auth: inherit
  body:
    type: json
    data: |
      {
        "order": 1,
        "product": 1,
        "quantity": 15,
        "unit_price": 2.75
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
