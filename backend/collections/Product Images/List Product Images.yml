info:
  name: List Product Images
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/products/1/images/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
