info:
  name: Get Category
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/api/categories/:category_id/"
  params:
    - name: category_id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
