info:
  name: Create Category
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/api/categories/"
  auth: inherit
  body:
    type: json
    data: |
      {
        "name": "Vegetables",
        "is_active": true
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
