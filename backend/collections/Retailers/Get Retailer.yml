info:
  name: Get Retailer
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/api/retailers/:retailer_id/"
  params:
    - name: retailer_id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
