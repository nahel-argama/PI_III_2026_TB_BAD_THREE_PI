info:
  name: Get Review
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/api/reviews/:review_id/"
  params:
    - name: review_id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
