info:
  name: List Reviews
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/reviews/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
