info:
  name: Cancel Order
  type: http
  seq: 8

http:
  method: POST
  url: "{{host}}/api/orders/:order_id/cancel/"
  params:
    - name: order_id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
