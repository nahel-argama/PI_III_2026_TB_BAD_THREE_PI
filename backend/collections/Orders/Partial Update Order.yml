info:
  name: Partial Update Order
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{host}}/api/orders/:order_id/"
  params:
    - name: order_id
      value: "1"
      type: path
  auth: inherit
  body:
    type: json
    data: |
      {
        "status": "DELIVERED"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
