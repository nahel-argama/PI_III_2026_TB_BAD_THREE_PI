info:
  name: Create Order
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/api/orders/"
  body:
    type: json
    data: |
      {
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
