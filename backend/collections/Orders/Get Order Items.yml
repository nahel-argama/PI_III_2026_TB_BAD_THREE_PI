info:
  name: Get Order Items
  type: http
  seq: 6

http:
  method: GET
  url: "{{host}}/api/orders/:order_id/items/"
  params:
    - name: order_id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
