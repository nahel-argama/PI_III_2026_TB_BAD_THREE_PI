info:
  name: Get Order
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/api/orders/:order_id/"
  params:
    - name: order_id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
