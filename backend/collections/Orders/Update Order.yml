info:
  name: Update Order
  type: http
  seq: 4

http:
  method: PUT
  url: "{{host}}/api/orders/:order_id/"
  params:
    - name: order_id
      value: "1"
      type: path
  auth: inherit
  body:
    type: json
    data: |
      {
        "status": "CONFIRMED"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
