info:
  name: Confirm Order
  type: http
  seq: 7

http:
  method: POST
  url: "{{host}}/api/orders/:order_id/confirm/"
  params:
    - name: order_id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
