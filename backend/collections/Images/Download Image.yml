info:
  name: Download Image
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/images/1/download/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
