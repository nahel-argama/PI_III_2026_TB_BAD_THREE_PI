info:
  name: Upload Product Image
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/api/products/:product-id/images/"
  params:
    - name: product-id
      value: ""
      type: path
  body:
    type: multipart-form
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
