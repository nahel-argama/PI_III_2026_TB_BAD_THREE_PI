info:
  name: List Product Images
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/products/:product-id/images/"
  params:
    - name: product-id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
