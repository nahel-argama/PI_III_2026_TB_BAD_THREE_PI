info:
  name: Get Product
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/api/products/:product_id/"
  params:
    - name: product_id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
