info:
  name: Create Wishlist Item
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/api/wishlists/items/"
  body:
    type: json
    data: |
      {
        "product_external_key": "108"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
