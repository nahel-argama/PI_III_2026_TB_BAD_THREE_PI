info:
  name: Delete Wishlist Item
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{host}}/api/wishlists/items/:item_id/"
  params:
    - name: item_id
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
