info:
  name: List Addresses
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/addresses/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
