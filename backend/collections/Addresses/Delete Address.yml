info:
  name: Delete Address
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{host}}/api/addresses/:address_id/"
  params:
    - name: address_id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
